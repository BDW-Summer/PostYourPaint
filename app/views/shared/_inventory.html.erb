<% if @inventory_items.any? %>
    <ol class="paints">
        <%= render partial: 'shared/inventory_items', collection: @inventory_items %>
    </ol>
    <%= will_paginate @inventory_items %>
<% else %>
    You don't have anything paint, <%= link_to "add some", paint_new_path %> now!
<% end %>
 