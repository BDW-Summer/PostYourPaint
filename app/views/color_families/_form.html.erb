<%= form_for(@color_family) do |f| %>
  <% if @color_family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@color_family.errors.count, "error") %> prohibited this color_family from being saved:</h2>

      <ul>
      <% @color_family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :family %><br />
    <%= f.text_field :family %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
